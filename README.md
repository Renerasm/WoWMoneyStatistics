
Working so far in prepatch 12.0, testing some more

Changes:
- Fixed event names that has changed
- Since Void bank no longer exists i disabled that code
- Disabled worldboss code, since it used combat log that is now restriced
